rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // For demo purposes only. Allows public read/write access.
      allow read, write: if request.auth != null;
    }
  }
}
